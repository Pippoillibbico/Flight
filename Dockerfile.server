FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY --chown=node:node . .
RUN mkdir -p /app/data/logs && chown -R node:node /app

ENV NODE_ENV=production
EXPOSE 3000

USER node
CMD ["npm", "run", "start"]
